<%=
  component_wrapper(style: "position: relative") do
    render(Primer::Alpha::ActionMenu.new(classes: "hide-when-print")) do |component|
      component.with_show_button(scheme: button_scheme, disabled: @disabled) do |button|
        button.with_leading_visual_icon(icon: :plus)
        t("outcome.add_button")
      end
      component.with_item(
        label: t("outcome.add_button"),
        tag: :a,
        content_arguments: {
          # href: new_outcome_meeting_agenda_item_path(@meeting, type: "simple", meeting_section_id: @meeting_section&.id),
          data: { "turbo-stream": true }
        }
      )
    end
  end
%>
