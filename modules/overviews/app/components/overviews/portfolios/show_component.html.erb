<%=
grid_layout("portfolios", tag: :div, align_items: :center) do |grid|
  grid.with_area(:kpi, tag: :div) do
  end

  grid.with_area(:subportfolio, tag: :div) do
  end

  grid.with_area(:budget, tag: :div) do
  end

  grid.with_area(:problems, tag: :div) do
  end
end
%>
