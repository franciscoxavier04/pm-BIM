The OP Heading component is a custom component that replaces the default Primer Heading' component. It allows you to use a standard HTML heading with appropriate styling for use within OpenProject.

## Overview

[CODE HERE]

## Anatomy

The OP Heading consists simply of a regular HTML h tag. It takes one input to specify which level of heading (from h1 to h6) should be used.

## Options

The OP Heading can show an optional divider at the bottom. 

This is useful in places like Settings pages that might need more visual differentiation between sections. 

## Used in

The Heading is used:

- Recurring meetings and meeting series index pages
- My account

## Examples


## Technical notes
